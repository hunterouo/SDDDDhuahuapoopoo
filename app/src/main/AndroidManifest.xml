<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wang.huntergod">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".firstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registerActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".loginmidActivity" />
        <activity
            android:name=".midinforActivity"
            android:label="@string/title_activity_midinfor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".editmidinforActivity" />
        <activity android:name=".AddCatActivity" />
        <activity android:name=".FixCatActivity"></activity>
        <uses-permission android:name="android.permission.CAMERA"/>
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    </application>

</manifest>